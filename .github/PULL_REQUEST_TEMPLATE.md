## What this changes

<!-- One or two sentences. Name the story in words, not by ticket key. -->

## Checklist

- [ ] Tests added or updated for every behaviour change
- [ ] ADR written, if the story requires one
- [ ] OpenAPI snapshot updated, if the contract changed
- [ ] Prompt eval attached, if a prompt changed
- [ ] `pnpm lint`, `pnpm typecheck`, `pnpm depcruise` and `pnpm test` pass locally
